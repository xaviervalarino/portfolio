#!/usr/bin/env node

/*
 * Create Project List
 * :::::::::::::::::::
 *
 * What it does:
 * Iterates through a markdown files in `dir`,
 * capturing the first couple of lines of YAML
 * which is then used to create an index/list for `outfile`
 *
 * Why?
 * If the title or subtitle of a file changes, it has to be changed
 * in two places. This automates that process.
 *
 * Note: must be run from project root!
 */

// Change current working directory to project root
// process.chdir('../')

const fs = require('fs');
const paths = {
  src: process.cwd() + '/src',
  dir: '/case-studies/',
}
paths.outfile = paths.src + '/work.md'

// An array of objects,
// each obj containing file 'title', 'subtitle', 'position' and 'href'
let fileList = [];

fs.readdir( (paths.src + paths.dir), (err, files) => {
  if (err) {
    console.log(err);
    process.exit(1);
  }
  files.forEach( (file, i) => {
    let last = false;
    // last file
    if ( files.length - 1 ===  i) {
      last = true;  
    }
    if ( file.split('.')[1] === 'md' ) {
      // streamFile(file)
      streamFile( file, last )
    }
  });
});

// change `.md` to `.html`
function createHref (file) {
  file = file.substr(0, file.lastIndexOf(".")) + ".html";
  return paths.dir + file;
}

// change strings into key/value pairs 
function massageData (data) {
  const obj = {};
  data.split('\n').slice(1,4)
   .forEach(textline => {
     const entry = textline.split(/: */)
     obj[ entry[0] ] = entry [1];
   });
  return obj;
}

// read the first couple of lines of the file
// convert it into an object and record it into the `fileList` array
function streamFile (file, lastFile) {
  const filepath = paths.src + paths.dir + file;
  const stream = fs.createReadStream(filepath, {
    highWaterMark: 150,
  });
  let fileInfo;
  let data = '';

  stream.on('data', (chunk) => {
    data += chunk;
    stream.destroy();
    fileInfo = massageData(data);
    fileInfo.href = createHref(file);
    fileList.push( fileInfo );
  });
  stream.on('close', () => {
    if ( lastFile ) writeOut();
  });
}
function writeOut () {
  // last bit of massaging
  fileList = fileList
    .filter( fileList => {
      return !isNaN( parseFloat(fileList.position) )
    })
    .sort( (prev, next) => {
      return prev.position - next.position 
    })
  ;
  writeToFile(fileList)
}

function writeToFile (fileList) {
  const stream = fs.createWriteStream(paths.outfile)
  //TODO: there has got to be an easy way to have this indented
  // but have the newlines stripped in the output
  const fileInfo = `---
# Generated by \`${__filename.substring(process.cwd().length + 1)}\`
# ${new Date()}
title: Case Studies
template: work.html
masthead:
    work: true
id: case-studies
projects:
`;
  stream.write(fileInfo);
  let outputList = ''
  fileList.forEach(obj => {
    Object.keys(obj).forEach( (key, i) => {
      // the dash allows Pandoc template to iterate through the list
      let space = '    - ';
      if( i !== 0 ) { space = '      '; };
      outputList += `${space}${key}: ${obj[key]}\n`;
    })
  })
  stream.write(outputList)
  stream.write('---')
}
